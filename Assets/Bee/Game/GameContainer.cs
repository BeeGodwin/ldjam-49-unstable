using UnityEngine;

namespace Bee.Game
{
    public class GameContainer : MonoBehaviour
    {
        void Start()
        {
            // install and instantiate the scene
        }

        // provide container as singleton
    
        // provide getters for depedencies
    }
}
